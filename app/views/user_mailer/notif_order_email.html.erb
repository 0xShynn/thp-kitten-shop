<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <h1>Salut à toi <%= @order.user.first_name %> et merci d'avoir passé une commande sur Kitten-Shop !</h1>
    <h2>Voici un résumé de ta commande n° <%= @order.id %> : </h2>
    <ul>
      <% @order.cart_items.each do |cart_item| %>
        <li>
          <h3><%= cart_item.item.name %></h3>
          <p><%= cart_item.item.description %></p>
        </li>
      <% end %>
    </ul>
    <p>Pour accéder à ton espace client, connecte-toi via : lol.</p>
  </body>
</html>